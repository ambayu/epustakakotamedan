import 'package:flutter/material.dart';

class perpusIcon {
  final String icon, slug;
  final String title;
  final Color? color;

  perpusIcon(
      {required this.icon,
      required this.title,
      this.color,
      required this.slug});
}
